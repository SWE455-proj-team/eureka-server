name: Deploy eureka-server

on:
  push:
  workflow_dispatch:
  
jobs:
  deploy:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/service-deploy.yml
    secrets: inherit
    with:
      service_name: eureka-server
      service_dir: .
